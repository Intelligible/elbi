"""Continuous statistical-pitfall benchmark harness.

Drives the elbi verification oracle over a catalog of curated (and real)
trap datasets and asserts each gate catches the pitfall a bare reading misses. It is
a top-level directory, not part of the shipped SDK, mirroring ``spec/``.
"""
