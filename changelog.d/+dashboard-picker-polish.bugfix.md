The tile editor's pickers drop below their field instead of jumping over the one above
it. Radix positions a select's list so the chosen item covers the trigger, which means
the list lands somewhere different depending on what is selected and can cover the
fields above. They now open below, left-aligned, at the field's width, and a long list
scrolls rather than filling the dialog.
